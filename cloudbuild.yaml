substitutions:
  _REGION: asia-northeast1
  _AR_REPO: citizen-dev-lab
  _APP_NAME: hello
  _SERVICE: app-hello
  _WORKFLOW: citizen-deploy
  _GH_OWNER: citizen-dev-lab
  _GH_REPO: citizen-apps

options:
  logging: CLOUD_LOGGING_ONLY

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/github-token/versions/latest
      env: GITHUB_TOKEN

steps:
  # 1) Docker build
  - name: gcr.io/cloud-builders/docker
    dir: apps/${_APP_NAME}
    args:
      - build
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_SERVICE}:$COMMIT_SHA
      - .

  # 2) Docker push
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_SERVICE}:$COMMIT_SHA

  # 3) Workflow → URL取得 → GitHubコメント
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: bash
    secretEnv: ["GITHUB_TOKEN"]
    args:
      - -c
      - |
        set -euo pipefail

        IMAGE="${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/${_SERVICE}:$COMMIT_SHA"

        EXEC_NAME=$(gcloud workflows run "${_WORKFLOW}" \
          --location="${_REGION}" \
          --data="{\"region\":\"${_REGION}\",\"serviceName\":\"${_SERVICE}\",\"image\":\"${IMAGE}\"}" \
          --format="value(name)")

        if [ -z "$EXEC_NAME" ]; then
          echo "Failed to start workflow"
          exit 1
        fi

        for i in $(seq 1 60); do
          STATE=$(gcloud workflows executions describe "$EXEC_NAME" \
            --location="${_REGION}" \
            --format="value(state)")

          if [ "$STATE" = "SUCCEEDED" ]; then
            RESULT=$(gcloud workflows executions describe "$EXEC_NAME" \
              --location="${_REGION}" \
              --format="value(result)")
            break
          fi

          if [ "$STATE" = "FAILED" ] || [ "$STATE" = "CANCELLED" ]; then
            echo "Workflow failed"
            exit 1
          fi

          sleep 3
        done

        URL=$(echo "$RESULT" | python3 -c 'import json,sys; print(json.load(sys.stdin).get("url",""))')

        COMMENT="✅ Deploy completed

- service: ${_SERVICE}
- region: ${_REGION}
- url: ${URL}
- image: ${IMAGE}"

        curl -s -X POST \
          -H "Authorization: Bearer $GITHUB_TOKEN" \
          -H "Accept: application/vnd.github+json" \
          "https://api.github.com/repos/${_GH_OWNER}/${_GH_REPO}/commits/$COMMIT_SHA/comments" \
          -d "{\"body\":\"$COMMENT\"}"

        echo "Posted deploy URL."
